<h1>Categories</h1>

<h3>List of all categories (sorted by name)</h3>

<table>
  <tr>
    <th>Name</th>
  </tr>
  <% @categories.each do |category| %>
  <tr>
    <td><%= category.name %></td>
  </tr>
  <% end %>
</table>
